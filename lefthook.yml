assert_lefthook_installed: true
colors: true
# lefthook.yml

output:
  - meta           # Print lefthook version
  - summary        # Print summary block (successful and failed steps)
  - empty_summary  # Print summary heading when there are no steps to run
  - success        # Print successful steps
  - failure        # Print failed steps printing
  - execution      # Print any execution logs (but prints if the execution failed)
  - execution_out  # Print execution output (but still prints failed commands output)
  - execution_info # Print `EXECUTE > ...` logging
  - skips          # Print "skip" (i.e. no files matched)
commit-msg:
  commands:
    commitlint:
      runner: npx commitlint --edit $1

# pre-commit:
#   parallel: true
#   commands:
#     prettier:
#       runner: npx prettier --write {staged_files}
#       glob: '**/*.{js,ts,vue,json,css,scss,md,yaml,yml}'
#       exclude: 'node_modules/**/*'
